<!DOCTYPE html>
<html lang="en">
  <head>
     <title>Shopping Bag</title>

    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Mr+Dafoe&family=Nunito:ital,wght@0,500;0,600;1,500&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
    <link rel="stylesheet" href="/vendor/border-box.css" />
    <link rel="stylesheet" href="/styles/main.css" />
     <link rel="stylesheet" href="/styles/layout.css" />
    <link rel="stylesheet" href="/styles/confirmation.css" />



    <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
  </head>

  <body>
    <%- include('partials/_header') %>
    <div class="container">
      <article class="card">
        <p class="card-header"> Your order is placed! You will shortly receive a text at <span class="number"> <%= customerInfo.phone %>  </span class="number"> message with the pick-up time.</p>
        <div class="card-body">
          <article class="card">
            <div class="card-body column">
              <div class="col"> <strong><%= customerInfo.name %>'s order contains: </strong></div>
            </div>
          </article>
          <hr>
          <ul class="column">
            <% for (let item in confirmedItems) { %>
              <% if (confirmedItems[item].qty > 0) { %>
              <li class="col-md-4">
                <figure class="itemside mb-3">
                  <div class="aside"><img src="<%= confirmedItems[item].image %>" class="img-sm border"></div>
                  <figcaption class="info align-self-center">
                    <p class="title"><%= item %><br> qty: <%= confirmedItems[item].qty %></p>
                  </figcaption>
                </figure>
              </li>
              <%} %>
            <% } %>
            <p><strong>Customer Notes:</strong> <%= customerInfo.notes %></p>
          </ul>
          <hr>
          <article class="card">
            <div class="card-body column">
              <div class="col">Subtotal: $<%= prices.subTotPrice %><br><strong>Total:$<%= prices.totPrice %></strong> </div>
            </div>
          </article>
        </div>
      </article>
    </div>
  </body>
</html>
